
machine:
  node:
    version: 0.10.38
  services:
    - redis
    - docker
  environment:
    VERSIONED: true

dependencies:
  pre:
   - npm -g install npm@2

deployment:
  push:
   branch: "2016-08-02"
   commands:
    - curl -sLu "$HTTP_AUTH:" $BUILD | bash
